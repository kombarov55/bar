(defpackage :add
  (:use :cl :main)
  (:export :proceed))

(defgeneric proceed (command)
  (:documentation "Выполнить комманду"))

(defmethod proceed (command)
  (format t "Такой команды не существует. Введите ещё раз."))

(defmethod proceed ((commmand (eql :exit)))
  (format t "До встречи!"))